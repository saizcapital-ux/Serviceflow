fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1
python-multipart==0.0.20
email-validator==2.2.0
reportlab==4.2.5
# Optional integrations (lazy-imported; only needed when enabled):
#   stripe>=11  — real Stripe subscription checkout (mock mode works without it)
#   boto3>=1.35 — S3 storage backend (local storage works without it)
